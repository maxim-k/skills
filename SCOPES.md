# Commit scopes

The closed vocabulary for the `<scope>` field in this repository's commit
messages. One scope per skill directory, plus two repository-level scopes. Add
a scope here when you add a skill.

- `conventional-commit`: the single-line Conventional Commit standard and its scope rules.
- `dockerfile-style`: multi-stage Dockerfile layout, build targets, and BuildKit cache mounts.
- `isabl-app`: scaffolding and self-checks for Isabl applications.
- `jupyter-notebook`: the analytical notebook standard, plots, and authoring workflow.
- `python-style`: the Python coding standard, architecture through logging.
- `vale-google-style`: the Vale setup and the Markdown prose lint loop.
- `readme`: the repository README and its skill index table.
- `repo`: repository-level files that are not a skill, such as this vocabulary and any tooling config.
